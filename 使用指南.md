# Note Digger 使用指南

## 一键启动

双击 `启动.bat`，等待 5 秒。

然后打开浏览器访问 **http://localhost:5000**

## 使用方法

1. 选择上传方式：**上传文件** / **粘贴链接** / **录制音频**
2. 选择引擎（推荐按顺序试）：
   - **Basic Pitch** — Spotify AI，准确度最好，推荐
   - **自动** — 智能选择最优引擎
   - **快速** — 极速处理，基础质量
3. 上传钢琴独奏音频（MP3/WAV/FLAC）
4. 等待 10-60 秒
5. 查看五线谱、在线播放、下载 MIDI/PDF

## 手动启动（如果 .bat 不行）

**终端 1 — 后端：**
```powershell
cd d:/GitWarehouse/note_digger_saier/backend
D:/Software/anaconda3/envs/bp311/python.exe -m uvicorn app.main:app --host 0.0.0.0 --port 8000
```

**终端 2 — 前端：**
```powershell
cd d:/GitWarehouse/note_digger_saier/frontend
npm run dev -- -p 5000
```

## 功能清单

- 音频上传（文件拖拽/URL/录音）
- 三级转录引擎（自动 / Basic Pitch / 快速）
- 实时进度条
- 五线谱渲染 (OSMD)
- 在线 MIDI 播放（Web Audio 合成）
- 和弦分析
- 左右手分离（高低音谱号）
- 多格式导出（PDF / MusicXML / MIDI / MP3）
- 免费试用框架

## 故障排除

| 问题 | 解决 |
|------|------|
| 前端显示"后端离线" | 确认后端终端在运行，端口 8000 |
| 端口被占用 | 关掉所有终端，重新双击启动.bat |
| 转录失败 | 确认 bp311 环境已配置，音频格式为 MP3/WAV |
| 播放只有 10 秒 | 刷新页面，已修复 |

## 技术栈

- AI: Basic Pitch (Spotify), Simple librosa
- 后端: FastAPI + Celery + Redis
- 前端: Next.js 16 + React 19 + TailwindCSS
- 乐谱: OpenSheetMusicDisplay + music21
